/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "lh3.googleusercontent.com",
        "th.bing.com",
        "images.unsplash.com",
        "res.cloudinary.com",
        "fakestoreapi.com"
      ],
    },
  };

module.exports = nextConfig
